/**
 * @file cli.h
 * @brief Інтерфейс інтерактивного CLI.
 */
#ifndef CLI_H
#define CLI_H

#include "cmd.h"

/**
 * Запустити інтерактивну оболонку керування пристроєм.
 *
 * Всі користувацькі команди вводяться вже всередині оболонки. Функція
 * повертає код завершення, отриманий від `cmd_device_shell()`.
 *
 * @return 0 успіх; ненульове значення при помилці.
 */
int cli_run_interactive (void);

#endif /* CLI_H */
